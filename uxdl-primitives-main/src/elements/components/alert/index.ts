import Root from './root.lite';
import Trigger from './trigger.lite';
import Overlay from './overlay.lite';
import Content from './content.lite';
import Title from './title.lite';
import Description from './description.lite';
import Action from './action.lite';
import Cancel from './cancel.lite';

const Alert = {
  Root: Root,
  Trigger: Trigger,
  Overlay: Overlay,
  Content: Content,
  Title: Title,
  Description: Description,
  Action: Action,
  Cancel: Cancel
};

export default Alert;
